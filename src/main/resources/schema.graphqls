type Query {
    bookById(id: ID): Book
    itemById(id: ID): Item
}

type Book {
    id: ID
    name: String
    pageCount: Int
    author: Author
}

type Author {
    id: ID
    firstName: String
    lastName: String
}

type Item {
    id: ID
    title: String
    thumbnail: String
}